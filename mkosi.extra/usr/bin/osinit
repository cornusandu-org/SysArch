#!/bin/sh

sudo pacman-key --init
sudo pacman-key --populate archlinux

sudo pacman -Sy --noconfirm --disable-download-timeout archlinux-keyring

sudo pacman -Syu base base-devel archlinux-tools --noconfirm
